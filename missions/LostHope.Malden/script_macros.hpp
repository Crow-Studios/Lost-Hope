#define STEAMID getPlayerUID player
#define UNIT player;



#define DEV getArray(missionConfigFile >> "staff" >> "developer");
#define MDEV getArray(missionConfigFile >> "staff" >> "mapDev");
#define ADMIN_INGAME getArray(missionConfigFile >> "staff" >> "admin");
#define TESTER getArray(missionConfigFile >> "staff" >> "tester");